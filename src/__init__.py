#We built this because whenever under setup.py find_packages is running it will go and search how many folder you have this __init__ is .
#It will directly consider src as source as a package itself and try to build and you can import it wherever you want.
